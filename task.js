function addToCalendarSlot() {
	/*
	if (slot available) {
		add to calendar
	} else {
		find another slot
		addTask()
	}
	*/
}
function schedule() {
	/*
	check due time
	addToCalendarSlot()
	change task.json
	*/
}
function adjourn() {
	/*
	if (now < due_time && (now + time_required) < (due_time - now)) {}
	*/
}
function assign() {
	/*
	only supervisor can assign
	*/
}